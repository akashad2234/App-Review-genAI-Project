# Frontend env (used by Next.js at build + runtime)
# For Vercel: set these in Project → Settings → Environment Variables

# Backend API base URL (no trailing slash)
NEXT_PUBLIC_API_BASE_URL=https://app-review-genai-project-production.up.railway.app
